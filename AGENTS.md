# yuhengyy.github.io 博客仓库指令

本仓是公开 MkDocs 叙述层。博客任务必须使用 `.agents/skills/blog-workspace/SKILL.md`。

- 不在本仓提交业务 `.cs`；Study C# 代码源是 `learning/src/Study/`。
- 私人材料属于 `publish/notes/`，不得加入 `mkdocs.yml`，公开前必须脱敏。
- 新页面同时更新 `mkdocs.yml` nav 和所属 `index.md`。
- `docs/workspace/repos.md` 是工作台生成镜像，不手改。
- 不提交 `site/`。
- 用户未明确要求时不 commit、不 push、不部署。

提交时使用中文说明和指定作者参数，不修改 Git config。
